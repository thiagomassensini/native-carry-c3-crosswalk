# Native Carry C3 Crosswalk v0.2.0

This release expands the exact finite real-to-C3 crosswalk into a typed
transport audit spanning enriched TFVD data, Green boundary forms,
camera-complete cutoffs, arithmetic nonlocal traces and full endpoint/bulk
reconstruction.

## Included

- exact Genuine bracket-to-Green identities before any zero assumption;
- the minimal provenance carrier and enriched pair transport retaining value,
  log jet, local cells, dormant edge, provenance and nonlocal tail;
- exact tail factorization by the nonzero C3 chart factor;
- camera-complete cutoff horizons for C2, odd and even natural cameras,
  including the explicit even antipodal endpoint atom;
- a positive Pythagorean branch-defect coordinate that cannot be cancelled by
  the scalar Genuine coordinates inside the completed ledger;
- the arithmetic nonlocal trace as a closed partial map with its exact domain
  obstruction;
- the form-first bridge from the finite prime-camera core to the intrinsic
  all-bases Gram completion;
- exact full-endpoint Poisson implications and finite witnesses separating a
  scalar-zero readout from a nonzero complete Green port;
- 335 public theorem reports, all restricted to the foundational allowlist
  `propext`, `Classical.choice`, and `Quot.sound`.

## Explicit boundary

The release does not infer that a scalar Genuine zero annihilates the complete
Green endpoint. It proves that this implication fails for the universal
linear carrier and isolates the only remaining locus: the nonlinear canonical
arithmetic curve whose blocks and log jet come from one common parameter.
No confinement theorem is claimed from the kernel-inclusion guardrail.

## Reproducibility

The release is built against Lean and Mathlib `v4.32.0` and exact Git commits
for every upstream package. The GitHub release is created only after the exact
`main` SHA passes the warning-as-error build, registry audit, Markdown audit,
trust-escape scan and all 335 named axiom reports.
