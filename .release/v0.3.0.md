# Native Carry C3 Crosswalk v0.3.0

This release promotes the kernel-checked bracket route developed after
`v0.2.0`. It records the exact relational identities and the strongest
noncircular analytic reductions currently available without claiming the
remaining completed trace theorem.

## Included

- the finite native/Genuine/TFVD/Green relational bracket law;
- the multiplicity-independent analytic-order mechanism reducing
  off-critical zero exclusion to a regular logarithmic-derivative identity;
- a canonical finite positive spectral measure and exact resolvent-transform
  representation for the raw C3 Cauchy readout;
- a kernel-checked no-go theorem showing that this raw positive scalar readout
  is not the completed Genuine logarithmic derivative;
- the exact polarized two-variable raw C3 Green--gamma factorization;
- explicit witnesses for the off-diagonal scalar-synthesis sector and the
  log-jet/raw-Green mismatch;
- the existing real-to-complex, enriched TFVD, complete-cutoff, arithmetic
  trace, and full-endpoint guardrails.

## Explicit boundary

This release does not claim unconditional global zero confinement. The raw C3
Green--gamma response is `3^(-s)`, not the completed response `-F'(s)/F(s)`.
The remaining gate is a single completed trace construction putting
`F(z)`, `-F'(z)`, and the spectral defect equation on the same state, or
equivalently the corresponding cross-multiplied two-variable Wronskian/Gram
identity.

## Reproducibility

GitHub Actions checks the exact commit, pinned Lean/Mathlib dependency graph,
warning-as-error build, theorem registry, Markdown, trust-escape scan, and
ordered axiom reports. The tag and release are created only after the exact
merged `main` SHA passes that audit.
